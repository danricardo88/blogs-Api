const User = require('./userController');
const Category = require('./categoriesController');
const BlogPosts = require('./postCategoriesController');

module.exports = {
  User,
  Category,
  BlogPosts,
};