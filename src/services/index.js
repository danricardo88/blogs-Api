const User = require('./user.service');
const Category = require('./categories.service');
const BlogPosts = require('./blogspots.service');
const PostCategory = require('./postcat.service');

module.exports = {
  User,
  Category,
  BlogPosts,
  PostCategory,
};